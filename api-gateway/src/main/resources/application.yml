server:
  port: 8080

spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: ${AUTH_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/auth/**

            - id: user-service
              uri: ${USER_SERVICE_URL:http://localhost:8082}
              predicates:
                - Path=/api/users/**

            - id: job-service
              uri: ${JOB_SERVICE_URL:http://localhost:8083}
              predicates:
                - Path=/api/jobs/**
              filters:
                - StripPrefix=1

            - id: candidate-service
              uri: ${CANDIDATE_SERVICE_URL:http://localhost:8084}
              predicates:
                - Path=/api/candidates/**

            - id: interview-service
              uri: ${INTERVIEW_SERVICE_URL:http://localhost:8085}
              predicates:
                - Path=/api/interviews/**

            - id: subscription-service
              uri: ${SUBSCRIPTION_SERVICE_URL:http://localhost:8086}
              predicates:
                - Path=/api/subscriptions/**

            - id: notification-service
              uri: ${NOTIFICATION_SERVICE_URL:http://localhost:8087}
              predicates:
                - Path=/api/notifications/**

            - id: file-service
              uri: ${FILE_SERVICE_URL:http://localhost:8088}
              predicates:
                - Path=/api/files/**

            - id: report-service
              uri: ${REPORT_SERVICE_URL:http://localhost:8089}
              predicates:
                - Path=/api/reports/**

          globalcors:
            corsConfigurations:
              '[/**]':
                allowedOrigins:
                  - "http://localhost:3000"
                  - "http://localhost:5173"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - PATCH
                  - DELETE
                  - OPTIONS
                allowedHeaders:
                  - "*"
                allowCredentials: true
          default-filters:
            - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

jwt:
  secret: ${JWT_SECRET:g7DoV5efMmZVdb23nYIYxPPHp1yngV6xGOdCeIN2AymRideshare2026SecureKeyForAllServices}